---
import { Image } from 'astro:assets'
import type { Image as ProjectsImage } from './Projects.astro'

interface Props {
  image: ProjectsImage
}

const { image } = Astro.props
---

<Image
  class:list={['max-w-none rounded-t-md shadow-2xl', image.class]}
  src={image.src}
  alt={image.alt}
/>
